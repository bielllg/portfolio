import React from 'react';

const Scanlines = () => {
    return (
        <div className="fixed inset-0 pointer-events-none z-[9999] opacity-20 bg-scanlines mix-blend-overlay" />
    );
};

export default Scanlines;
